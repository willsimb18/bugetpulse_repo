# Dependencies
node_modules/

# Build output
app/dist/
app/dev-dist/

# Local env — never commit; VITE_* values belong in Vercel env vars.
# Covers .env, .env.dev, .env.prod, .env.local ... but keeps the template.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/
