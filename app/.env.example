# Supabase -> Project Settings -> API
# Project URL
VITE_SUPABASE_URL=
# anon / public key (NOT service_role — that must never reach the browser)
VITE_SUPABASE_ANON_KEY=

# Optional. Minutes of inactivity before sign-out; defaults to 60.
# 0.5 = 30 seconds, for testing.
VITE_IDLE_MINUTES=
